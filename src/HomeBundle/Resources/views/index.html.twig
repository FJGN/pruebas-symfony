{% extends 'base.html.twig' %}

{% block body %}

    <div class="panel panel-info">
        <div class="panel-heading"> 
            <h2>Inicio</h2>
        </div>

        <div class="panel-body">
            <form action="{{url('login')}}">
                <button type="submit" class="btn btn-primary">Ingresar en la aplicaci&oacute;n</button>
            </form>
            <br/>
            <form action="{{url('registro')}}">
                <button type="submit" class="btn btn-success">Registro en la aplicaci&oacute;n</button>
            </form>
        </div>
    </div>

{% endblock %}
